{#
  Minimal on purpose. The tests read the resolved value and the attributes the
  shape wires onto the root, not markup, so this only has to render without
  erroring.
#}
<div{{ attributes }}>{{ heading }}</div>
