{#
  Minimal on purpose. The suite asserts against stored and merged values,
  not markup.
#}
<div{{ attributes }}>
  <div class="na-top">{{ top }}</div>
  <div class="na-bottom">{{ bottom }}</div>
</div>
